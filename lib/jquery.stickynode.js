// Generated by CoffeeScript 1.3.3

(function($, window) {
  var StickyNode, clone, defaults, document, pluginName;
  pluginName = 'stickynode';
  document = window.document;
  clone = null;
  defaults = {
    offsetY: 0,
    offsetX: 0
  };
  StickyNode = (function() {

    function StickyNode(element, options) {
      this.element = element;
      this.options = $.extend({}, defaults, options);
      this._defaults = defaults;
      this._name = pluginName;
      this.init();
    }

    StickyNode.prototype.init = function() {
      var node, rectangle, xAndY;
      node = $(this.element);
      xAndY = node.offset();
      clone = node.clone().hide().appendTo(node.parent());
      rectangle = {
        position: 'fixed',
        top: xAndY.top,
        left: xAndY.left,
        width: node.width(),
        height: node.height()
      };
      clone.css(rectangle);
      $(window).scroll(this.window_scroll);
      clone.data('#{pluginName}.top', rectangle.top);
    };

    StickyNode.prototype.window_scroll = function() {
      var top;
      top = clone.data('#{pluginName}.top');
      if ($(window).scrollTop() > top) {
        clone.show();
      } else {
        clone.hide();
      }
    };

    return StickyNode;

  })();
  return $.fn[pluginName] = function(options) {
    return this.each(function() {
      if (!$.data(this, "plugin_" + pluginName)) {
        return $.data(this, "plugin_" + pluginName, new StickyNode(this, options));
      }
    });
  };
})(jQuery, window);
